# Settings toolchain test plan

CI must at minimum compile the WinForms application and complete startup smoke. Runtime install/setup paths require Windows integration testing because they invoke folder selection, Git/dotnet, DepotDownloader and Steam authentication.
