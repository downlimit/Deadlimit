# Deadlimit

Deadlimit is a Deadlock character-replacement modding toolchain intended to automate extraction, project preparation, material authoring setup, compilation, post-processing, VPK packaging, and deployment.

See:

- `PROJECT.md` for product behavior and user workflow;
- `ARCHITECTURE.md` for technical constraints and known-good toolchain facts;
- `ROADMAP.md` for staged implementation.
