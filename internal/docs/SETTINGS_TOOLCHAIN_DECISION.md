# Settings toolchain decision

The Settings UI treats Reduced CSDK and DeadlockTools as managed dependencies, Retail Deadlock as a located external installation, and Projects folder as a workspace. CSDK generation numbers are displayed as version state rather than embedded in the stable dependency label.
